{
  "name": "degreetrackr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "preview": "cd frontend && npm run preview",
    "build": "cd frontend && npm install --include=dev && npm run build",
    "check:pages-release": "npm run build && node scripts/check_pages_release.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "convex": "^1.32.0"
  }
}
